extends template
block content
    div
        | Hello #{userName}
        br
        | Welcome on board, your Email: #{userMail}.
        br
        | Please activate your profile. 👇
        br
        form(action=`${activateUrl}` method="POST")
            button(type="submit" class='btn' ) Click here to approve your email.
